% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge.R
\name{bridge}
\alias{bridge}
\alias{tick}
\title{Let a coding agent run code in your session through a file}
\usage{
bridge(enable = TRUE, confirm = TRUE)

tick()
}
\arguments{
\item{enable}{\code{TRUE} to install the callback, \code{FALSE} to remove it.}

\item{confirm}{\code{TRUE} (default) to prompt y/n before each run.}
}
\value{
Invisibly \code{TRUE} when the bridge is on afterwards, \code{FALSE} when off.

\code{tick()} invisibly returns \code{TRUE} if it ran a command, \code{FALSE} if
there was nothing pending or it was declined.
}
\description{
A fallback for when every customized MCP server is disabled and the agent
cannot reach your session at all. Once you call \code{bridge()}, any R code the
agent writes to \code{.rcontext/command.R} is evaluated \strong{in your global
environment} the next time you run a console command (or use \code{\link[=tick]{tick()}} / the
RStudio addin), and its output is written to \code{.rcontext/result.txt} for the
agent to read. The code and its output are also echoed to your console.
}
\details{
By default it \strong{asks y/n, showing the code, before running}, so nothing
from the agent executes without your say-so on that specific command. Say no
and the command is set aside (\code{command.R.declined}) and the agent is told.
Pass \code{confirm = FALSE} for a session where you would rather it just run.

Even with \code{confirm = FALSE} this is a supervised channel: it advances only on
your action, the queued file is visible until it runs so deleting it cancels
the command, and everything happens in \code{globalenv()} where you can see and
undo it. It is still arbitrary code execution in a live session holding your
real work. Treat \code{rm()}, \code{file.remove()}, \code{system()} and overwriting a
loaded object as needing your explicit say-so, exactly as with \code{run_r}.

Off by default. Put \code{rcontext::bridge()} in your \verb{~/.Rprofile} (after
\code{rcontext::start()}) to have it on in every session.

\code{tick()} runs a pending \code{.rcontext/command.R} immediately, without waiting
for you to enter a console command. Bind it to an RStudio keyboard shortcut
through \strong{Addins}, or call it directly. It obeys the same \code{confirm} setting.
}
\examples{
\dontrun{
rcontext::bridge()                 # asks before each run
rcontext::bridge(confirm = FALSE)  # runs queued code without prompting
# ... agent writes .rcontext/command.R ...
rcontext::tick()                   # or run anything at the console
rcontext::bridge(FALSE)
}
}
