% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{teardown}
\alias{teardown}
\title{Undo setup()}
\usage{
teardown(
  agents = c("claude", "copilot"),
  rprofile = path.expand("~/.Rprofile"),
  claude_dir = path.expand("~/.claude")
)
}
\arguments{
\item{agents}{Agent CLIs to register with. Ones not found on \code{PATH} are
skipped with a note.}

\item{rprofile}{Path to the startup file to modify. Exposed for testing;
there is no reason to change it.}

\item{claude_dir}{Path to Claude Code's configuration directory, which is
where its skill is written. Exposed for testing, as \code{rprofile} is.}
}
\value{
Invisibly, a list recording what was removed.
}
\description{
Removes the startup block from your \verb{~/.Rprofile}, unregisters the MCP
server from each agent CLI found on \code{PATH}, and removes the skill that
\code{\link[=setup]{setup()}} installed. Lines you wrote yourself are left exactly as they were.
}
\examples{
\dontrun{
rcontext::teardown()
}
}
