% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot.R
\name{export}
\alias{export}
\title{Export objects for analysis outside the live session}
\usage{
export(..., max_size = 50 * 1024^2, dir = opt_object_dir())
}
\arguments{
\item{...}{Objects to export, named or quoted: \code{export(model, penguins)} or
\code{export("model", "penguins")}.}

\item{max_size}{Per-object ceiling in bytes. A larger object is skipped with
a message rather than written. Default 50 MB.}

\item{dir}{Destination directory. Defaults to \code{.rcontext/objects}.}
}
\value{
Invisibly, the paths written.
}
\description{
Writes one \code{.rds} file per named object into \verb{.rcontext/objects/}. When the
MCP broker cannot reach your session, an agent can still start its own
\code{Rscript}, \code{readRDS()} one of these files and analyse the copy, without ever
touching your session. Read-only by construction.
}
\details{
Object selection is explicit and never automatic. Serialising a dataset to
disk in a corporate setting is a data-governance decision, so this writes
only the objects you name, and skips any larger than \code{max_size}.

The files land under \verb{.rcontext/}, which is kept out of git by a \code{.gitignore}
written there on first use.
}
\examples{
\dontrun{
rcontext::export(model, penguins)
}
}
