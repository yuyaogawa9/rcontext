% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{setup}
\alias{setup}
\title{Set up rcontext once, for every project}
\usage{
setup(
  agents = c("claude", "copilot"),
  rprofile = path.expand("~/.Rprofile"),
  claude_dir = path.expand("~/.claude")
)
}
\arguments{
\item{agents}{Agent CLIs to register with. Ones not found on \code{PATH} are
skipped with a note.}

\item{rprofile}{Path to the startup file to modify. Exposed for testing;
there is no reason to change it.}

\item{claude_dir}{Path to Claude Code's configuration directory, which is
where its skill is written. Exposed for testing, as \code{rprofile} is.}
}
\value{
Invisibly, a list recording what changed.
}
\description{
Does the things a package alone cannot: arranges for future R sessions to
register themselves, tells your coding agents how to reach them, and tells
those agents when to bother.
}
\details{
Specifically it
\enumerate{
\item appends a small guarded block to your \verb{~/.Rprofile}, between markers, so
every interactive R session calls \code{\link[=start]{start()}};
\item registers \code{rcontext} as an MCP server with each agent CLI it finds on
your \code{PATH}, at user scope, by calling that CLI's own \verb{mcp add} command;
and
\item installs a skill describing when to use the tools and how to read their
output, so an agent reaches for your live session unprompted instead of
guessing from your source files.
}

It shows you every change and asks before making any of them. \code{\link[=teardown]{teardown()}}
reverses them. Re-running \code{setup()} is safe: the startup block is added at
most once, and the skill is refreshed from the installed package, so
upgrading \code{rcontext} and re-running picks up newer guidance.
}
\examples{
\dontrun{
rcontext::setup()
}
}
